import React from 'react';

// TODO Requires css-loader which requires webpack config
// import Spinner from 'react-spinkit';

export default () => (
  <div>Loading</div>
  // <Spinner spinnerName='circle' />
);
